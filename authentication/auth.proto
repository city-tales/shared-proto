syntax = "proto3";
package authVerification;

message AuthVerification {
    bool isEmailVerified = 1;
    bool isGoogleSignedIn = 2;
    bool isAppleSignedIn = 3;
    bool isPasswordLess = 4;
    bool isMFAEnabled = 5;
}